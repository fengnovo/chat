#### 推送本地代码
```
cd /Users/keen/Desktop/code/projects/chat && rsync -av --delete --progress \
  --exclude '.env' \
  --exclude 'deploy/.env' \
  --exclude 'node_modules/' \
  --exclude 'apps/desktop/' \
  --exclude 'docs/' \
  --exclude 'apps/worker/data/' \
  --exclude 'packages/ai-cli/sessions/' \
  --exclude '.superpowers' \
  --exclude '.turbo' \
  --exclude '.worktrees' \
  --exclude '.github' \
  --exclude '.trace' \
  --exclude '.pnpm-store' \
  --exclude '.next/' \
  --exclude 'dist/' \
  --exclude 'data/' \
  --exclude '.git' \
  ./ root@101.96.208.160:/opt/chat/

```

#### 部署
```
ssh root@101.96.208.160

cd /opt/chat
bash deploy/scripts/app-build.sh

//或者
pnpm build
systemctl restart chat-api chat-web chat-worker chat-knowledge

```

#### 连接生产环境Langfuse和Grafana
```
ssh -N \
  -L 13300:127.0.0.1:33005 \
  -L 13909:127.0.0.1:39090 \
  root@101.96.208.160

Langfuse http://127.0.0.1:33005


ssh -N \
  -L 33000:127.0.0.1:33006 \
  -L 39090:127.0.0.1:39090 \
  root@101.96.208.160
Grafana：http://127.0.0.1:33006
Prometheus：http://127.0.0.1:39090

```

#### 构建客户端线上包
```
// 提交代码
git tag desktop-v0.1.1
git push origin desktop-v0.1.1
```
